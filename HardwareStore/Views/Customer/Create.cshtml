@model HardwareStore.Data.Entities.Customer

@{
    ViewData["Title"] = "Crear Cliente";
}

<style>
    body {
        background-color: #f8f9fa;
    }

    .main-container {
        display: flex;
        justify-content: right;
        align-items: right;
        min-height: 100vh;
        margin-top: -50px; /* Agrega esta línea */
    }

    .card {
        width: 100%;
        max-width: 1000px;
        margin: 20px;
        padding: 20px;
    }

    .btn-custom {
        background-color: #007bff;
        color: white;
        border-radius: 30px;
        padding: 10px 20px;
        font-size: 16px;
    }

        .btn-custom:hover {
            background-color: #0056b3;
        }

    .btn-outline-custom {
        border-color: #007bff;
        color: #007bff;
        border-radius: 30px;
        padding: 10px 20px;
        font-size: 16px;
    }

        .btn-outline-custom:hover {
            background-color: #007bff;
            color: white;
        }

    .form-label {
        font-size: 18px;
    }

    .form-control {
        font-size: 16px;
        padding: 10px;
    }
</style>

<div class="main-container">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white text-center">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="card-body">
            <form asp-action="Create" method="post">
                <div class="mb-3">
                    <label asp-for="FirstName" class="form-label">Nombre</label>
                    <input asp-for="FirstName" class="form-control" placeholder="Nombre.." />
                    <span asp-validation-for="FirstName" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="LastName" class="form-label">Apellido</label>
                    <input asp-for="LastName" class="form-control" placeholder="Apellido.." />
                    <span asp-validation-for="LastName" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Customeraddress" class="form-label">Dirección</label>
                    <input asp-for="Customeraddress" class="form-control" placeholder="Dirección.." />
                    <span asp-validation-for="Customeraddress" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Phone" class="form-label">Teléfono</label>
                    <input asp-for="Phone" class="form-control" placeholder="Teléfono.." />
                    <span asp-validation-for="Phone" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-custom">Guardar</button>
                    <button type="button" class="btn btn-outline-custom" onclick="window.location.href='/Customer/Index'">Volver</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
